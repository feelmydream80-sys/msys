# V1.21.1 변경사항 백업 (rebase 복원 전)
# 생성일: 2026-04-09
# 참고: 이 파일의 내용들은 복원 후 다시 적용 필요

## 1. 설명(desc) displayMode 추가

### templates/collection_schedule.html (radio button 추가)
```html
<label style="cursor: pointer; display: flex; align-items: center; gap: 0.25rem;">
    <input type="radio" name="displayMode" value="desc"> 설명
</label>
```

### static/js/pages/collection_schedule.js

#### mstData 저장 방식 변경 (line 1116-1124):
```javascript
mstData = activeMstResult.reduce((acc, item) => {
    acc[item.job_id] = {
        cd_nm: item.cd_nm,
        cd_desc: item.cd_desc ? item.cd_desc.replace(/_/g, ' ') : ''
    };
    return acc;
}, {});
```

#### 상위 그룹 카드 (line 673-697):
```javascript
const displayMode = document.querySelector('input[name="displayMode"]:checked').value;
let parentDisplayName = parentGroupName;
if (displayMode === 'name' && mstData[parentGroupName]) {
    parentDisplayName = mstData[parentGroupName].cd_nm;
} else if (displayMode === 'desc' && mstData[parentGroupName]) {
    parentDisplayName = mstData[parentGroupName].cd_desc || parentGroupName;
}

// innerHTML에서 overflow: ellipsis 적용
groupPill.innerHTML = `
    <div style="display: flex; justify-content: space-between; align-items: center;">
        <span style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">${parentDisplayName} <span class="expand-icon">▶</span></span>
        ...
    </div>
    ...
`;
groupPill.title = parentDisplayName;
```

#### 하위 그룹 카드 (line 757-769):
```javascript
let subDisplayName = subGroupId;
if (displayMode === 'name' && mstData[subGroupId]) {
    subDisplayName = mstData[subGroupId].cd_nm;
} else if (displayMode === 'desc' && mstData[subGroupId]) {
    subDisplayName = mstData[subGroupId].cd_desc || subGroupId;
}

subGroupPill.innerHTML = `
    <div style="font-size: 0.85rem; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">${subDisplayName}</div>
    ...
`;
subGroupPill.title = subDisplayName;
```

#### 팝업 내 개별 job (하위 그룹 팝업, line 790-813):
```javascript
let jobDisplayName = job.job_id;
if (popupDisplayMode === 'name' && mstData[job.job_id]) {
    jobDisplayName = mstData[job.job_id].cd_nm;
} else if (popupDisplayMode === 'desc' && mstData[job.job_id]) {
    jobDisplayName = mstData[job.job_id].cd_desc || job.job_id;
}
// 시간 추가 로직 유지
if (sameDayJobs.length > 1) {
    const timePart = (job.date.split(' ')[1] || "");
    if (timePart) {
        const hour = parseInt(timePart.substring(0, 2), 10);
        jobDisplayName += `(${hour}시)`;
    }
}
```

#### 일간 히트맵 개별 job (line 1072-1080):
```javascript
let jobDisplayName = job.job_id;
if (displayMode === 'name' && mstData[job.job_id]) {
    jobDisplayName = mstData[job.job_id].cd_nm;
} else if (displayMode === 'desc' && mstData[job.job_id]) {
    jobDisplayName = mstData[job.job_id].cd_desc || job.job_id;
}
const maxLength = 12;
if (jobDisplayName.length > maxLength) {
    jobDisplayName = jobDisplayName.substring(0, maxLength) + '...';
}
```

---

## 2. 그룹 메모 기능 (이미 Staging된 파일들)

### DDL/tb_grp_memo.sql
- tb_grp_memo 테이블 생성 (grp_id, memo_date, depth, content, wrtr_id, wrte_dt, updtr_id, upd_dt)

### DDL/tb_con_hist_update.sql
- tb_con_hist에서 trbl_hist_no 열 삭제

### dao/grp_memo_dao.py
- GrpMemoDao 클래스

### mapper/grp_memo_mapper.py
- GrpMemoMapper 클래스 (CRUD)

### routes/ui/collection_schedule_routes.py
- GET /api/group-memo - 조회
- POST /api/group-memo - 생성
- PUT /api/group-memo - 수정
- DELETE /api/group-memo - 삭제

### static/js/pages/collection_schedule.js (메모 관련)
- updateMemoButtons() 함수 - 메모 버튼 상태 업데이트
- 메모 버튼 클릭 시 팝업 표시/숨기기
- memoPopup 이벤트 처리

### templates/collection_schedule.html
- 메모 팝업 HTML 추가 (form, textarea, buttons)